# __init__.py — Paquete de ingestión de normativa (scraper, parser, chunker, embedder, persistencia).
"""Normativa ingestion: scraper, parser, chunker, embedder, persistence."""

from app.ingestion.cli import main as main
